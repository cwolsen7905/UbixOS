# bin/detachtest/Makefile — detached-thread teardown test (Task E2b).

include ../../Makefile.incl
include ../Makefile.incl

BINARY = detachtest

OBJS = main.o

.include "${UBIX_MK}/ubix.musl.prog.mk"
