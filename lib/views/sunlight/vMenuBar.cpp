#include <vContext.h>
#include <vMenuBar.h>

vMenuBar::vMenuBar(vContext * parent) : vContext(parent) {
  return;
} // vMenuBar::vMenuBar

vMenuBar::~vMenuBar(void) {
  return;
} // vMenuBar::~vMenuBar
