/* Stub of NetSurf core's utils/log.h for the standalone uBixOS libnsfb build.
 * libnsfb sources use LOG((...)) (double-paren); make it a no-op. */
#ifndef NSFB_UBIX_STUB_LOG_H
#define NSFB_UBIX_STUB_LOG_H
#define LOG(x) ((void)0)
#endif
