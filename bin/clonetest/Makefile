# bin/clonetest/Makefile — end-to-end test for rfork()/clone() thread creation.

include ../../Makefile.incl
include ../Makefile.incl

BINARY = clonetest

OBJS = main.o clone_test.o

.include "${UBIX_MK}/ubix.musl.prog.mk"
