# bin/ls/Makefile — full self-contained POSIX ls (libc only; no busybox/libbb).
include ../../Makefile.incl
include ../Makefile.incl

BINARY = ls
OBJS   = ls.o

.include "${UBIX_MK}/ubix.musl.prog.mk"
