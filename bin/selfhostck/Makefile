# bin/selfhostck/Makefile — self-host foundation check (Phase 0).
include ../../Makefile.incl
include ../Makefile.incl

BINARY = selfhostck
OBJS   = main.o
SRCS   = main.c

.include "${UBIX_MK}/ubix.musl.prog.mk"
