# bin/pthreadtest/Makefile — first end-to-end musl pthreads test (Task E).

include ../../Makefile.incl
include ../Makefile.incl

BINARY = pthreadtest

OBJS = main.o

.include "${UBIX_MK}/ubix.musl.prog.mk"
